name: Apply AI Patch

on:
  push:
    branches:
      - "custom"
    paths:
      - ".ai-patches/*.diff"
      - ".ai-patches/*.patch"
      - ".ai-patches/*.zip"

permissions:
  contents: write

concurrency:
  group: ai-patch-${{ github.ref_name }}
  cancel-in-progress: false

jobs:
  apply-patch:
    if: github.actor != 'github-actions[bot]' && github.ref_name == 'custom'
    runs-on: ubuntu-latest

    steps:
      - name: Checkout target branch
        uses: actions/checkout@v7
        with:
          ref: ${{ github.ref_name }}
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}
          persist-credentials: true

      - name: Find patch file
        id: patch
        run: |
          PATCH_FILE=$(git diff-tree --no-commit-id --diff-filter=AM --name-only -r "${{ github.sha }}" | grep -E '^.ai-patches/.*\.(diff|patch|zip)$' | tail -n 1 || true)

          if [ -z "$PATCH_FILE" ]; then
            echo "No patch file found in current commit."
            echo "has_patch=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          PATCH_NAME=$(basename "$PATCH_FILE")
          META_FILE="${PATCH_FILE%.*}.json"
          PACKAGE_TYPE="${PATCH_FILE##*.}"

          echo "has_patch=true" >> "$GITHUB_OUTPUT"
          echo "patch_file=$PATCH_FILE" >> "$GITHUB_OUTPUT"
          echo "patch_name=$PATCH_NAME" >> "$GITHUB_OUTPUT"
          echo "meta_file=$META_FILE" >> "$GITHUB_OUTPUT"
          echo "package_type=$PACKAGE_TYPE" >> "$GITHUB_OUTPUT"

      - name: Read patch metadata
        id: metadata
        if: steps.patch.outputs.has_patch == 'true'
        env:
          META_FILE: ${{ steps.patch.outputs.meta_file }}
          PACKAGE_TYPE: ${{ steps.patch.outputs.package_type }}
          PATCH_NAME: ${{ steps.patch.outputs.patch_name }}
        run: |
          TITLE_FILE="$RUNNER_TEMP/openpatch-commit-title.txt"
          node - "$META_FILE" "$TITLE_FILE" "$PACKAGE_TYPE" "$PATCH_NAME" <<'NODE'
          const fs = require("fs");
          const [metaFile, titleFile, packageType, patchName] = process.argv.slice(2);
          let title = "";
          if (fs.existsSync(metaFile)) {
            const metadata = JSON.parse(fs.readFileSync(metaFile, "utf8"));
            title = String(metadata.title || "").replace(/[\r\n]+/g, " ").trim();
          }
          if (!title && packageType === "zip") {
            title = `ai: apply changed files package ${patchName}`;
          }
          if (!title) {
            title = `ai: apply ${patchName}`;
          }
          fs.writeFileSync(titleFile, `${title}\n`);
          NODE
          echo "title_file=$TITLE_FILE" >> "$GITHUB_OUTPUT"

      - name: Apply changed files zip
        if: steps.patch.outputs.has_patch == 'true' && steps.patch.outputs.package_type == 'zip'
        env:
          ZIP_FILE: ${{ steps.patch.outputs.patch_file }}
        run: |
          EXTRACT_DIR="$RUNNER_TEMP/openpatch-files"
          mkdir -p "$EXTRACT_DIR"

          python3 - "$ZIP_FILE" <<'PY'
          import os
          import stat
          import sys
          import zipfile

          zip_path = sys.argv[1]

          def validate_path(name):
            path = name.replace("\\", "/").strip()
            if not path or path.endswith("/"):
              return
            if path.startswith("__MACOSX/") or path.endswith(".DS_Store"):
              return
            if path.startswith("/") or path.startswith("../") or "/../" in path or path == "..":
              raise ValueError(f"Unsafe zip path: {name}")
            if path.startswith(".git/") or path == ".git":
              raise ValueError(f"Unsafe zip path: {name}")
            if path.startswith(".ai-patches/") or path == ".ai-patches":
              raise ValueError(f"Unsafe zip path: {name}")
            if path.startswith(".github/workflows/") or path == ".github/workflows":
              raise ValueError(f"Workflow changes are not allowed from zip: {name}")

          with zipfile.ZipFile(zip_path) as archive:
            for entry in archive.infolist():
              validate_path(entry.filename)
              mode = (entry.external_attr >> 16) & 0o170000
              if stat.S_ISLNK(mode):
                raise ValueError(f"Symlinks are not allowed in zip: {entry.filename}")
          PY

          unzip -q "$ZIP_FILE" -d "$EXTRACT_DIR"
          node .github/scripts/openpatch/apply-zip.mjs "$EXTRACT_DIR"

      - name: Normalize patch
        id: normalized
        if: steps.patch.outputs.has_patch == 'true' && steps.patch.outputs.package_type != 'zip'
        env:
          PATCH_FILE: ${{ steps.patch.outputs.patch_file }}
        run: |
          NORMALIZED_PATCH="$RUNNER_TEMP/openpatch-normalized.patch"
          LOOSE_APPLIER=".github/scripts/openpatch/apply-loose-patch.mjs"

          node .github/scripts/openpatch/normalize-patch.mjs "$PATCH_FILE" "$NORMALIZED_PATCH"

          echo "patch_file=$NORMALIZED_PATCH" >> "$GITHUB_OUTPUT"
          echo "loose_applier=$LOOSE_APPLIER" >> "$GITHUB_OUTPUT"

      - name: Validate patch
        id: apply_mode
        if: steps.patch.outputs.has_patch == 'true' && steps.patch.outputs.package_type != 'zip'
        env:
          NORMALIZED_PATCH: ${{ steps.normalized.outputs.patch_file }}
          LOOSE_APPLIER: ${{ steps.normalized.outputs.loose_applier }}
        run: |
          for STRIP in "" -p0 -p1 -p2 -p3 -p4 -p5 -p6 -p7 -p8 -p9 -p10 -p11 -p12; do
            if git apply $STRIP --check "$NORMALIZED_PATCH"; then
              echo "mode=git" >> "$GITHUB_OUTPUT"
              echo "strip=$STRIP" >> "$GITHUB_OUTPUT"
              echo "already_applied=false" >> "$GITHUB_OUTPUT"
              exit 0
            fi
          done

          for STRIP in "" -p0 -p1 -p2 -p3 -p4 -p5 -p6 -p7 -p8 -p9 -p10 -p11 -p12; do
            if git apply $STRIP --reverse --check "$NORMALIZED_PATCH"; then
              echo "Patch has already been applied. Temporary patch files will be removed."
              echo "mode=git" >> "$GITHUB_OUTPUT"
              echo "strip=$STRIP" >> "$GITHUB_OUTPUT"
              echo "already_applied=true" >> "$GITHUB_OUTPUT"
              exit 0
            fi
          done

          if node "$LOOSE_APPLIER" --check "$NORMALIZED_PATCH"; then
            echo "mode=loose" >> "$GITHUB_OUTPUT"
            echo "strip=" >> "$GITHUB_OUTPUT"
            echo "already_applied=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          echo "Patch cannot be applied with tested strip levels."
          exit 1

      - name: Apply patch
        if: steps.patch.outputs.has_patch == 'true' && steps.apply_mode.outputs.already_applied != 'true' && steps.apply_mode.outputs.mode == 'git'
        env:
          NORMALIZED_PATCH: ${{ steps.normalized.outputs.patch_file }}
          STRIP: ${{ steps.apply_mode.outputs.strip }}
        run: |
          if [ -n "$STRIP" ]; then
            git apply "$STRIP" "$NORMALIZED_PATCH"
          else
            git apply "$NORMALIZED_PATCH"
          fi

      - name: Apply loose patch
        if: steps.patch.outputs.has_patch == 'true' && steps.apply_mode.outputs.already_applied != 'true' && steps.apply_mode.outputs.mode == 'loose'
        env:
          NORMALIZED_PATCH: ${{ steps.normalized.outputs.patch_file }}
          LOOSE_APPLIER: ${{ steps.normalized.outputs.loose_applier }}
        run: |
          node "$LOOSE_APPLIER" "$NORMALIZED_PATCH"

      - name: Remove temporary patch files
        if: steps.patch.outputs.has_patch == 'true'
        env:
          PATCH_FILE: ${{ steps.patch.outputs.patch_file }}
          META_FILE: ${{ steps.patch.outputs.meta_file }}
        run: |
          git rm -f "$PATCH_FILE" || true
          git rm -f "$META_FILE" || true

      - name: Commit applied changes
        if: steps.patch.outputs.has_patch == 'true'
        env:
          TITLE_FILE: ${{ steps.metadata.outputs.title_file }}
        run: |
          git config user.name "openpatch-bot"
          git config user.email "openpatch-bot@users.noreply.github.com"

          git add .

          if git diff --cached --quiet; then
            echo "No changes to commit."
            exit 1
          fi

          .github/scripts/openpatch/commit-patch.sh "$TITLE_FILE"

      - name: Push target branch
        if: steps.patch.outputs.has_patch == 'true'
        env:
          TARGET_BRANCH: ${{ github.ref_name }}
        run: |
          if [ "$TARGET_BRANCH" != "custom" ]; then
            echo "Refusing to write any branch other than custom: $TARGET_BRANCH"
            exit 1
          fi
          git push origin HEAD:custom
